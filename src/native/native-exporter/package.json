{
  "name": "native-exporter",
  "version": "1.0.0",
  "private": true,
  "description": "Native D3D11 + NVENC export engine",
  "main": "build/Release/native_exporter.node",
  "dependencies": {
    "node-addon-api": "^8.3.0"
  },
  "scripts": {
    "build": "node-gyp rebuild --arch=x64",
    "configure": "node-gyp configure --arch=x64"
  }
}
