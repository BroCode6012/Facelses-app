{
  "name": "gpu-export",
  "version": "1.0.0",
  "private": true,
  "description": "V2 GPU-native export addon: ANGLE D3D11 interop + NVENC encoding",
  "main": "build/Release/gpu_export.node",
  "dependencies": {
    "node-addon-api": "^8.3.0"
  },
  "scripts": {
    "build": "node-gyp rebuild --arch=x64",
    "configure": "node-gyp configure --arch=x64"
  }
}
